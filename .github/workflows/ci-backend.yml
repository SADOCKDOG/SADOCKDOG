name: 🐍 Backend CI - SADOCKDOG
on:
  push:
    branches: [ master, dev ]
    paths:
      - 'autogpt_platform/backend/**'
      - 'autogpt_platform/autogpt_libs/**'
      - '.github/workflows/ci-backend.yml'
  pull_request:
    branches: [ master, dev ]
    paths:
      - 'autogpt_platform/backend/**'
      - 'autogpt_platform/autogpt_libs/**'
jobs:
  lint-format:
    name: 🔍 Lint & Format
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      - name: 🐍 Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: 📦 Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: 💾 Cache Poetry dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pypoetry
          key: ${{ runner.os }}-poetry-${{ hashFiles('autogpt_platform/backend/poetry.lock') }}
      - name: 📚 Install dependencies
        working-directory: autogpt_platform/backend
        run: poetry install
      - name: 🔎 Run Ruff linter
        working-directory: autogpt_platform/backend
        run: poetry run ruff check .
      - name: 🎨 Check code formatting
        working-directory: autogpt_platform/backend
        run: |
          poetry run black --check .
          poetry run isort --check-only .
  test:
    name: 🧪 Tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: test_password_sadockdog
          POSTGRES_DB: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      redis:
        image: redis:latest
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      - name: 🐍 Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: 📦 Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: 💾 Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pypoetry
          key: ${{ runner.os }}-poetry-${{ hashFiles('autogpt_platform/backend/poetry.lock') }}
      - name: 📚 Install dependencies
        working-directory: autogpt_platform/backend
        run: poetry install
      - name: 🗄️ Run migrations
        working-directory: autogpt_platform/backend
        env:
          DATABASE_URL: postgresql://postgres:test_password_sadockdog@localhost:5432/postgres?schema=platform
          DIRECT_URL: postgresql://postgres:test_password_sadockdog@localhost:5432/postgres?schema=platform
        run: |
          poetry run prisma generate
          poetry run prisma migrate deploy
      - name: 🧪 Run tests
        working-directory: autogpt_platform/backend
        env:
          DATABASE_URL: postgresql://postgres:test_password_sadockdog@localhost:5432/postgres?schema=platform
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          ENCRYPTION_KEY: test_encryption_key_for_ci_only
          DIRECT_URL: postgresql://postgres:test_password_sadockdog@localhost:5432/postgres?schema=platform
        run: |
          poetry run pytest -v --maxfail=3 --tb=short \
            --ignore=backend/blocks/airtable \
            --ignore=backend/blocks/test \
            --ignore=backend/data/credit_ceiling_test.py \
            --ignore=backend/data/credit_concurrency_test.py \
            --ignore=backend/data/credit_integration_test.py \
            --ignore=backend/data/credit_metadata_test.py \
            --ignore=backend/data/credit_refund_test.py \
            --ignore=backend/data/credit_underflow_test.py \
            --ignore=backend/data/credit_user_balance_migration_test.py \
            --ignore=backend/executor/manager_low_balance_test.py \
            --ignore=backend/server/routers/analytics_improved_test.py \
            --ignore=backend/server/routers/analytics_parametrized_test.py \
            --ignore=backend/server/routers/analytics_test.py \
            --ignore=backend/server/v2/admin \
            --ignore=backend/server/v2/otto \
            --ignore=backend/server/v2/turnstile \
            --ignore=backend/util/cloud_storage_test.py \
            --ignore=backend/util/virus_scanner_test.py \
            --ignore=test/blocks \
            --ignore=test/sdk \
            backend/blocks/test/test_block.py \
            backend/data/model_test.py \
            backend/data/graph_test.py \
            backend/util/test_json.py \
            backend/util/decorator_test.py
      - name: ✅ Tests passed
        run: echo "🎉 All backend tests passed!"
